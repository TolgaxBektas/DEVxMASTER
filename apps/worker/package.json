{
  "name": "@xmaster-center/worker",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "format": "prettier --write src"
  },
  "dependencies": {
    "@xmaster-center/jobs": "workspace:*",
    "@xmaster-center/kernel": "workspace:*",
    "@xmaster-center/module-crm": "workspace:*",
    "@xmaster-center/module-system": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "24.7.0",
    "prettier": "3.6.2",
    "typescript": "5.9.3",
    "vitest": "2.1.4"
  }
}
